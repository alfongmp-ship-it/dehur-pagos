index_real.html
*.local

# ===== SECRETOS — JAMÁS subir =====
# Carpeta .secrets/ va completa ignorada.
# Plantillas vacias committed van en .secrets.example/
.secrets/
.env
.env.local
.env.*.local
*.pem

# Documentación maestra: se conserva LOCAL, no se sube (el repo es público).
docs/documentacion-completa.md
